import { createTheme } from '@mui/material';

export const theme = createTheme({
  typography: {
    fontFamily: 'TTNorms-Regular',
  },
  palette: {
    mode: 'dark',
    primary: {
      main: '#16d741',
    },
  },
});
